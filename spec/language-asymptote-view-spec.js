'use babel';

import LanguageAsymptoteView from '../lib/language-asymptote-view';

describe('LanguageAsymptoteView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
